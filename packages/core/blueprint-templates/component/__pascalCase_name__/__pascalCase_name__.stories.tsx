import React from 'react';
import Bedrock{{pascalCase name}} from './{{pascalCase name}}';

import type { Props } from './{{pascalCase name}}.types';

export default {
  title: '{{pascalCase name}}',
  component: Bedrock{{pascalCase name}},
  argTypes: {},
};

export const {{pascalCase name}} = (props: Props) => (
  <Bedrock{{pascalCase name}} {...props} />
);
